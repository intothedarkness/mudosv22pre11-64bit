all:
	@echo MUD-OS is currently configured to use 'GNUmakefile'.  Edit 'build.MUD-OS'
	@echo if this is not what you want.

.DEFAULT:
	@echo MUD-OS is currently configured to use 'GNUmakefile'.  Edit 'build.MUD-OS'
	@echo if this is not what you want.

nothing:

which_makefile:
	echo MakeIsMake
